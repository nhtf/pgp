send heartbeat for chat messages
highlight links in chat (linkify) (<span>)
add ability to mention in chat (and invite with it)
fix duplicate status updates being send
title bar does not get updated correctly when logging in

Fix chatroom options size
fix gameroom updates (m: fix what?)
find out how revolt detects embed type and do something similar

random team names
darker offline color in friend list

AMMOERROR (chen)
Canvas fullscreen for VR and escape (or something like that) to exit vr -> to prevent memory leak from webgl canvas stuff

Profile page stat part

Match History page (either on profile or seperate page?)

chat invite for game (also how to determine which kind of game)

matchmaking system!!

huge text (c: is dit voor de embeds?)

Member entity (en andere?) hebben dubbele IDs in DB, kan beter via relations
