export enum Subject {
	USER,
	INVITE,
	FRIEND,
	ROOM,
	MEMBER,
}

export enum Action {
	ADD,
	SET,
	REMOVE,
}

export enum Role {
	MEMBER,
	ADMIN,
	OWNER,
};

export enum Access {
	PUBLIC,
	PROTECTED,
	PRIVATE,
};

export enum Gamemode {
	REGULAR,
	VR,
	MODERN
};

export enum Status {
	OFFLINE,
	IDLE,
	ACTIVE,
}

export enum CoalitionColors {
	CETUS = "3AA2DB",
	PYXIS = "B73188",
	VELA = "E52A2D",
}